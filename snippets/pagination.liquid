{% comment %}

  This snippet renders a custom widget based on the current 'paginate' context,
  defined in { % paginate XXXX by 5 % } tags wrapping the content.

  Replace Text Example:
    <div class="pagination">
      {{ paginate | default_pagination | replace: 'Previous', 'Newer articles' | replace: 'Next', 'Older articles'  }}
    </div>

  More information:
   - http://docs.shopify.com/themes/liquid-variables/paginate

{% endcomment %}

  <!-- <div class="{% if pagination %}twelve{% else %}sixteen{% endif %} columns"> -->
    <div class="{% if pagination %}full-width-cgc{% else %}sixteen{% endif %}">
    <div class="section clearfix">
      <div class="paginate">
        {% if paginate.pages > 1 %}
          {{ paginate | default_pagination | replace: '&raquo;', '<span class="icon-right-arrow"><i class="fas fa-caret-right paginate-right"></i></span>' | replace: '&laquo;', '<span class="icon-left-arrow"><i class="fas fa-caret-left paginate-left"></i></span>' }}
        {% endif %}
      </div>
    </div>
  </div>
